package cs3500.animator.controller;

/**
 * Represents the interface for a controller for an animation.
 */
public interface IController {
  /**
   * Runs the current animation based on how the controller sees fit.
   */
  void start();
}
